🚀 AUTOMATION SYSTEM WORKFLOW DIAGRAM
==========================================

📁 FILES INVOLVED:
├── simple_main.py (Entry Point)
├── config_manager.py (Configuration)
├── automation.py (Main Automation)
├── window_manager.py (Window Management)
├── workflow.py (Action Execution)
├── ui_detection.py (UI Detection)
├── error_handler.py (Error Handling)
├── config.json (App Settings)
└── instructions.json (Automation Objectives)

🔄 EXECUTION FLOW:
==================

1️⃣ ENTRY POINT
   ┌─────────────────────────────────┐
   │ simple_main.py                 │
   │ ┌─────────────────────────────┐ │
   │ │ def main():                 │ │
   │ │   print("🚀 Starting...")  │ │
   │ └─────────────────────────────┘ │
   └─────────────────────────────────┘
                    │
                    ▼

2️⃣ CONFIGURATION LOADING
   ┌─────────────────────────────────┐
   │ config_manager = ConfigManager()│
   │ ┌─────────────────────────────┐ │
   │ │ config_manager.py           │ │
   │ │ ├── __init__()              │ │
   │ │ ├── load_config()            │ │
   │ │ └── get_app_config()        │ │
   │ └─────────────────────────────┘ │
   └─────────────────────────────────┘
                    │
                    ▼

3️⃣ APP SELECTION
   ┌─────────────────────────────────┐
   │ if len(sys.argv) > 1:          │
   │   app_name = sys.argv[1]       │
   │ else:                          │
   │   app_name = get_default_app() │
   └─────────────────────────────────┘
                    │
                    ▼

4️⃣ AUTOMATION MODE SELECTION
   ┌─────────────────────────────────┐
   │ run_automation(app_config)      │
   │ ┌─────────────────────────────┐ │
   │ │ if use_template_mode:       │ │
   │ │   run_template_automation() │ │
   │ │ else:                       │ │
   │ │   run_standard_automation() │ │
   │ └─────────────────────────────┘ │
   └─────────────────────────────────┘
                    │
                    ▼

5️⃣ STANDARD AUTOMATION PATH
   ┌─────────────────────────────────┐
   │ SimpleAppAutomation()          │
   │ ┌─────────────────────────────┐ │
   │ │ automation.py               │ │
   │ │ ├── __init__()              │ │
   │ │ ├── run()                   │ │
   │ │ ├── prepare_application()   │ │
   │ │ └── execute_objectives()    │ │
   │ └─────────────────────────────┘ │
   └─────────────────────────────────┘
                    │
                    ▼

6️⃣ WINDOW MANAGEMENT
   ┌─────────────────────────────────┐
   │ window_manager.py               │
   │ ┌─────────────────────────────┐ │
   │ │ ├── launch_app()           │ │
   │ │ │   ├── get_existing_windows()│
   │ │ │   ├── is_running()        │ │
   │ │ │   └── subprocess.Popen()  │ │
   │ │ ├── focus_window()          │ │
   │ │ └── maximize_window()       │ │
   │ └─────────────────────────────┘ │
   └─────────────────────────────────┘
                    │
                    ▼

7️⃣ OBJECTIVE EXECUTION
   ┌─────────────────────────────────┐
   │ workflow.py                    │
   │ ┌─────────────────────────────┐ │
   │ │ ├── parse_objectives()     │ │
   │ │ ├── execute_objective()    │ │
   │ │ ├── _execute_action()       │ │
   │ │ │   ├── _type_text()        │ │
   │ │ │   ├── _hotkey()           │ │
   │ │ │   ├── _key_press()        │ │
   │ │ │   └── _delete_text()      │ │
   │ │ └── _check_prerequisites()  │ │
   │ └─────────────────────────────┘ │
   └─────────────────────────────────┘
                    │
                    ▼

8️⃣ UI DETECTION & VERIFICATION
   ┌─────────────────────────────────┐
   │ ui_detection.py                 │
   │ ┌─────────────────────────────┐ │
   │ │ ├── verify_element_present()│ │
   │ │ ├── find_template()         │ │
   │ │ ├── click_at_location()     │ │
   │ │ └── cleanup_screenshots()   │ │
   │ └─────────────────────────────┘ │
   └─────────────────────────────────┘
                    │
                    ▼

9️⃣ ERROR HANDLING
   ┌─────────────────────────────────┐
   │ error_handler.py               │
   │ ┌─────────────────────────────┐ │
   │ │ ├── log_error()             │ │
   │ │ ├── send_email_notification()│
   │ │ └── handle_retry()          │ │
   │ └─────────────────────────────┘ │
   └─────────────────────────────────┘
                    │
                    ▼

🔟 RESULTS & CLEANUP
   ┌─────────────────────────────────┐
   │ finish_automation(success)      │
   │ ┌─────────────────────────────┐ │
   │ │ if success:                 │ │
   │ │   print("🎉 Success!")      │ │
   │ │ else:                       │ │
   │ │   print("❌ Failed!")       │ │
   │ │   sys.exit(1)               │ │
   │ └─────────────────────────────┘ │
   └─────────────────────────────────┘

📊 DATA FLOW:
=============

config.json ──┐
              ├──► ConfigManager ──► App Selection
instructions.json ──┘

App Selection ──► WindowManager ──► App Launch/Focus

App Launch ──► WorkflowExecutor ──► Action Execution

Action Execution ──► UI Detection ──► Verification

Verification ──► ErrorHandler ──► Email Notifications

📧 EMAIL NOTIFICATIONS:
======================

┌─────────────────────────────────┐
│ .env file                       │
│ ├── EMAIL_ENABLED=true          │
│ ├── SMTP_SERVER=smtp.gmail.com  │
│ ├── SMTP_USERNAME=your-email    │
│ ├── SMTP_PASSWORD=your-password │
│ └── TO_EMAIL=saul.vera787@gmail.com│
└─────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────┐
│ Email Triggers:                 │
│ ├── App launch failure          │
│ ├── Unsupported objectives      │
│ ├── Action execution failure    │
│ └── Critical errors             │
└─────────────────────────────────┘

🎯 COMMAND LINE USAGE:
=====================

python simple_main.py                    # Uses default app
python simple_main.py Notepad           # Uses specific app
python simple_main.py Notepad obj1,obj2 # Uses specific objectives

📁 FILE DEPENDENCIES:
====================

simple_main.py
├── config_manager.py
├── automation.py
│   ├── window_manager.py
│   ├── workflow.py
│   ├── ui_detection.py
│   └── error_handler.py
├── config.json
└── instructions.json

🔧 CONFIGURATION FILES:
======================

config.json:
├── apps (Notepad, Calculator, Paint)
├── default_app
├── settings
└── instructions_file

instructions.json:
├── objectives
├── workflow_templates
├── retry_policies
└── verification_settings
